#ifndef MENSCH_AERGERE_DICH_NICHT_HPP
#define MENSCH_AERGERE_DICH_NICHT_HPP

/**
 * Main entry point to the application.
 * Creates needed objects and starts the logic.
 */
int main(int argc, char **argv);

#endif